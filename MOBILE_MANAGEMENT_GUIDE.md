# Руководство по управлению блоками на мобильных устройствах

## Как управлять содержимым и размерами блоков

### 1. 📱 Адаптивные классы Tailwind CSS

Для управления размерами и содержимым блоков на разных экранах используются следующие префиксы:
- **sm:** - экраны от 640px
- **md:** - экраны от 768px  
- **lg:** - экраны от 1024px
- **xl:** - экраны от 1280px

### 2. 🎯 Основные параметры для изменения

#### Отступы (Padding):
```css
py-12 md:py-20        /* Вертикальные отступы: 48px на мобильных, 80px на десктопе */
px-4 md:px-6          /* Горизонтальные отступы */
```

#### Размеры текста:
```css
text-2xl md:text-4xl lg:text-5xl  /* Заголовки */
text-sm md:text-base lg:text-lg   /* Обычный текст */
```

#### Сетки и колонки:
```css
grid-cols-1 sm:grid-cols-2 lg:grid-cols-3  /* Количество колонок */
gap-4 md:gap-6 lg:gap-8                    /* Промежутки между элементами */
```

### 3. 📍 Где находятся настройки блоков

#### Главная страница (src/pages/Index.tsx):
- **Строки 177-186**: Секция услуг - заголовки и отступы
- **Строки 346-355**: Портфолио - размеры изображений
- **Строки 375-384**: Прайс-лист - адаптивные колонки
- **Строки 450-461**: Контакты - форма обратной связи
- **Строки 592-644**: Футер - адаптивная сетка

#### Компонент "О нас" (src/components/CompanyDescription.tsx):
- **Строки 31-44**: Заголовки и отступы секции
- **Строки 79-88**: Карточки преимуществ
- **Строки 97-107**: Блок с зоной работы

#### Команда (src/components/Team.tsx):
- **Строки 37-46**: Заголовок секции
- **Строка 48**: Сетка карточек команды

#### Отзывы (src/components/Reviews.tsx):
- **Строки 79-88**: Заголовок и сетка отзывов
- **Строки 127-129**: Статистика

### 4. 🛠️ Как изменить размеры блоков

#### Уменьшить блок на мобильных:
```jsx
// Было:
<section className="py-20">

// Стало:
<section className="py-8 md:py-20">
```

#### Изменить количество колонок:
```jsx
// Было:
<div className="grid md:grid-cols-3 gap-8">

// Стало:
<div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4 md:gap-8">
```

#### Адаптивные размеры текста:
```jsx
// Было:
<h2 className="text-4xl font-light">

// Стало:
<h2 className="text-2xl md:text-4xl lg:text-5xl font-light">
```

### 5. 📝 Примеры изменения содержимого

#### Скрыть элемент на мобильных:
```jsx
<div className="hidden md:block">
  Этот контент виден только на больших экранах
</div>
```

#### Показать разный контент:
```jsx
<span className="block md:hidden">Короткий текст</span>
<span className="hidden md:block">Подробный текст для десктопа</span>
```

#### Адаптивные изображения:
```jsx
<img className="w-full h-48 md:h-64 lg:h-80 object-cover" />
```

### 6. 🎨 Адаптивные стили для элементов

#### Кнопки:
```jsx
<Button className="text-sm px-4 py-2 md:text-base md:px-6 md:py-3">
```

#### Карточки:
```jsx
<Card className="p-4 md:p-6 lg:p-8">
```

#### Сетки:
```jsx
<div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4 md:gap-6">
```

### 7. 🔧 Быстрые настройки для популярных изменений

#### Сделать блок компактнее на мобильных:
1. Найдите `py-20` и замените на `py-12 md:py-20`
2. Найдите `mb-16` и замените на `mb-8 md:mb-16`
3. Найдите `gap-8` и замените на `gap-4 md:gap-8`

#### Уменьшить размер текста:
1. `text-4xl` → `text-2xl md:text-4xl`
2. `text-xl` → `text-lg md:text-xl`
3. `text-lg` → `text-sm md:text-lg`

#### Изменить сетку:
1. `grid-cols-3` → `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3`
2. `md:grid-cols-2` → `grid-cols-1 md:grid-cols-2`

### 8. 📋 Чек-лист для проверки мобильной адаптации

- [ ] Текст читается без горизонтальной прокрутки
- [ ] Кнопки достаточно большие для тапа (минимум 44px)
- [ ] Изображения масштабируются правильно
- [ ] Отступы не слишком большие на маленьких экранах
- [ ] Контент помещается в viewport
- [ ] Навигация доступна через бургер-меню

### 9. 🎯 Точки контроля (breakpoints)

- **Mobile**: 320px - 639px (без префикса)
- **Tablet**: 640px - 767px (sm:)
- **Desktop Small**: 768px - 1023px (md:)
- **Desktop Large**: 1024px+ (lg:, xl:)

### 10. 💡 Полезные советы

1. **Тестируйте на реальных устройствах** - браузерные DevTools не всегда показывают реальную картину
2. **Начинайте с мобильных** - design mobile-first подход
3. **Используйте относительные единицы** - rem, em, %, vh, vw
4. **Проверяйте производительность** - на мобильных важна скорость загрузки
5. **Учитывайте touch-интерфейс** - элементы должны быть достаточно большими для пальцев